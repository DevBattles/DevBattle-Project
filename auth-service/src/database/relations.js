// ===========================================
// Database Relations
// ===========================================

/**
 * Table relations for Drizzle ORM.
 * Currently the auth service has a single users table with no foreign key relations.
 * As the system grows, additional relations can be defined here.
 */

// import { relations } from 'drizzle-orm';
// import { users } from './schema.js';

// Example future relation:
// export const usersRelations = relations(users, ({ many }) => ({
//   sessions: many(sessions),
// }));

export {};
